<div class="container-fluid px-4 py-4 text-center">
    <div class="mt-5">
        <i class='bx bx-lock-alt text-warning' style="font-size: 80px;"></i>
        <h2 class="mt-3 fw-bold text-dark">Caja Cerrada</h2>
        <p class="text-muted fs-5">No puedes realizar ventas porque no hay una sesión de caja abierta para tu usuario.</p>
        <button class="btn btn-primary btn-lg mt-3 fw-bold" onclick="$('.nav-link[data-url=\'@Url.Action("Index", "Caja")\']').trigger('click');">
            Ir a Abrir Caja
        </button>
    </div>
</div>
<script>
    // Automáticamente intentar redirigir si el usuario no hace clic
    setTimeout(() => {
        $('.nav-link[data-url="@Url.Action("Index", "Caja")"]').trigger('click');
    }, 3000);
</script>
